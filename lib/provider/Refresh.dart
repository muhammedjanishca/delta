 Future<void> refresh() async {
    // Simulate a network request or any asynchronous operation
    await Future.delayed(Duration(seconds: 2));

    // setState(() {
    //   // Update the data
    //   items = List.generate(10, (index) => 'Item $index (refreshed)');
    // });
  }